<h1><%= @post.title %></h1>
<h2>By <%= link_to @post.user.username, user_path(@post.user) %></h2>
<p><%= @post.content %></p>
<%= link_to "Back to posts", posts_path%><br><br>
<% @post.comments.each do |comment| %>
  <%= comment.content %><br>
  <p>-<%= link_to comment.user.username, user_path(comment.user) %></p><br>
<% end %>
